declare type MouseCoords = {
    x: number;
    y: number;
};
declare type CursorCoords = {
    x: number;
    y: number;
};
declare type MouseAction = (cursorElement: HTMLDivElement, shapeElement: HTMLDivElement) => void;
export default class Cursor {
    cursorElement: HTMLDivElement;
    shapeElement: HTMLDivElement | null;
    bounds: DOMRect;
    mouseCoords: MouseCoords;
    cursorCoords: CursorCoords;
    speed: number;
    animationFrame: number | null;
    isAnimating: boolean;
    constructor(el: HTMLDivElement, onMouseDown?: MouseAction, onMouseUp?: MouseAction);
    run(): void;
    stop(): void;
    private getBounds;
    private animateCursor;
    private onMouseMove;
    private onMouseDown;
    private onMouseUp;
}
export {};
