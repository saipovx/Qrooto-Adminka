"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nearlyEqual = exports.lerp = void 0;
var lerp = function (from, to, amount) {
    amount = amount < 0 ? 0 : amount;
    amount = amount > 1 ? 1 : amount;
    return from + (to - from) * amount;
};
exports.lerp = lerp;
/**
 * Checks that both numbers are within a range.
 * The default range is 1 because the units passed to this are pixels, and with
 * lerp values the last pixel never really arrives at the target.
 * @param a
 * @param b
 * @param targetDiff
 * @returns
 */
var nearlyEqual = function (a, b, targetDiff) {
    if (targetDiff === void 0) { targetDiff = 1; }
    return Math.abs(a - b) < targetDiff;
};
exports.nearlyEqual = nearlyEqual;
