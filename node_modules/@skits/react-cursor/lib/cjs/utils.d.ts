export declare const lerp: (from: number, to: number, amount: number) => number;
/**
 * Checks that both numbers are within a range.
 * The default range is 1 because the units passed to this are pixels, and with
 * lerp values the last pixel never really arrives at the target.
 * @param a
 * @param b
 * @param targetDiff
 * @returns
 */
export declare const nearlyEqual: (a: number, b: number, targetDiff?: number) => boolean;
